name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --check
      - run: cargo clippy --all-targets --release -- -D warnings
      # heavy ntuple tests build multi-GB tables; cap parallelism so
      # overlapping allocations fit in runner memory (flaky OOM otherwise)
      - run: cargo test --release -- --test-threads=2
      - run: cargo build --profile wasm-release --target wasm32-unknown-unknown --lib
